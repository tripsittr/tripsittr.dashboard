// Builder JS permanently removed. This file is intentionally a no-op placeholder
// to avoid build/runtime issues for any lingering references.

// No-op builder shim
window.triplinkBuilderPresent = false;

export default {};
// Kept as an empty stub so Vite/asset imports don't fail if any references remain.

// No runtime behavior.

